import 'package:flutter/material.dart';

class GeolocationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Placeholder UI for geolocation screen
    return Scaffold(
      appBar: AppBar(
        title: Text('Nearby Pharmacies'),
      ),
      body: Center(
        child: Text(
          'Geolocation screen coming soon.',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
